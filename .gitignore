# macOS
.DS_Store

# Editor
.obsidian/

# Large PDF files (source papers - uncomment if you want to track them)
# raw/sources/*.pdf

# Optional: if you use LFS for PDFs
# *.pdf filter=lfs diff=lfs merge=lfs -text
